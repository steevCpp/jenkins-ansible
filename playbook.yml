---
- name: copy war
hosta: all
become: yes
vars:
  source=./webapp/target/webapp.war
  destination=/var/
tasks:
  - name: install tomcat
    apt: install 
  -name: copy
   copy:
    -src: "{{soucre}}"
    -dest: "{{destination}}"
